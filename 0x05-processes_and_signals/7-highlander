#!/usr/bin/env bash
# A Bash script that handles the SIGTERM signal and prints "OK" when received

# Trap the SIGTERM signal and print "OK"
trap 'echo "OK";' SIGTERM

# Infinite loop that prints "To infinity and beyond" every 2 seconds
while true
do
  echo "To infinity and beyond"
  sleep 2
done
