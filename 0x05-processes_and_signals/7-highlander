#!/usr/bin/env bash
# A Bash script that runs infinitely and handles SIGTERM by printing "OK"

trap 'echo "OK"' SIGTERM  # When SIGTERM is received, it prints "OK"

while true
do
  echo "To infinity and beyond"
  sleep 2
done
